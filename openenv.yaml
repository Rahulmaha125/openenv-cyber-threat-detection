name: cyber-threat-detection-env

description: AI environment for detecting cyber security threats from network logs.

entry_point: environment:CyberThreatEnv

observation_space:
  type: text
  key: network_log

action_space:
  type: discrete
  actions:
    - block_ip
    - monitor
    - allow

reward_range:
  min: 0.0
  max: 1.0
